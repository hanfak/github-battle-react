var React = require('react');

var Guest = React.createClass({
  render: function () {
    return (
      <div>
        Hello from Guest!
        <p>MORe random stuff</p>
      </div>
    )
  }
});

module.exports = Guest;
